# DroneGSC ↔ UAVLink-Edge Implementation Protocol Mapping

**Document Version:** 2.1.0  
**Reference Specification:** [`docs/ACTUAL_UAVLINK_EDGE_PROTOCOL.md`](file:///c:/Users/Admin/Documents/app%20drone/docs/ACTUAL_UAVLINK_EDGE_PROTOCOL.md)  
**Date:** 2026-08-25  

---

| # | Requirement | Source-of-truth requirement | Current implementation | Gap | Required change | Verification method | Status |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **1** | **Control Topology Support** | Must support both Mode 1 (Direct Wi-Fi: Pi IP :14550) and Mode 2 (4G Cloud: `45.117.171.237:14550`). | `ControlConnectionService.ts` implements `resolveTargetEndpoint()` supporting `WIFI_DIRECT` and `CELLULAR_4G`. | Presets needed clean binding in connection profile. | Keep endpoints in `src/settings/defaults/connection.ts` and allow runtime switching. | Unit test + Network handover test | **COMPLIANT** |
| **2** | **Port Integrity** | Pi ↔ Pixhawk: UART 921600 / ETH 14550. Forwarding: UDP 14550. Web API: 8080. | Control port `14550` is standard; Web Admin is `8080`. | None. | Enforce standard port constants without hardcoded string literals. | Static code audit + Jest test | **COMPLIANT** |
| **3** | **Control Transport Pipeline** | Pipeline: `Joystick UI` $\rightarrow$ `JoystickProcessor` $\rightarrow$ `SafetyLayer` $\rightarrow$ `ControlConnectionService` $\rightarrow$ `MAVLink UDP :14550` $\rightarrow$ `Pixhawk`. | Joystick sends via `SafetyLayer` $\rightarrow$ `ControlConnectionService`. | Need explicit documentation of pipeline and fail-closed gates. | Ensure `SafetyLayer` is the final gate before `ControlConnectionService.sendCommand()`. | SafetyLayer unit test | **COMPLIANT** |
| **4** | **Joystick 10–20 Hz Loop & Neutral Safety** | Continuous 10–20 Hz tick. Timeout $>500$ ms forces neutral (`roll=0, pitch=0, yaw=0, throttle=0.5`). REST NEVER used for joystick. | `JoystickProcessor.ts` continuously emits safe neutral frames upon touch release or 500ms timeout. | None. | Maintain continuous tick and immediate failsafe neutralization on stick release or control loss. | Jest async tick test | **COMPLIANT** |
| **5** | **MAVLink Mission Handshake** | Synchronized: `IDLE` $\rightarrow$ `CLEAR_ALL` $\rightarrow$ `COUNT_SENT` $\rightarrow$ `WAITING_REQUEST` $\rightarrow$ `SENDING_ITEM(seq)` $\rightarrow$ `WAITING_ACK` $\rightarrow$ `SYNCED`. | `MissionService.ts` implements request-response synchronized upload loop with timeout & retry. | Ensure exact sequence validation for each `MISSION_REQUEST_INT(seq)` and handle duplicate requests. | Implement strict request-response state machine with 3 retries max. | Mission handshake unit test | **COMPLIANT** |
| **6** | **Mission Start Interlock** | `MISSION_START` must verify mission count $>0$, verify AUTO mode (switch & wait heartbeat up to 8s) before start. | `MissionService.ts` (`startMission()`) implements mission count validation, AUTO mode check & 8s heartbeat wait. | None. | Interlock matches `forwarder.py` lines 648–657. | Mock AUTO interlock test | **COMPLIANT** |
| **7** | **Video Protocol Priority & WebRTC Audit** | PRIMARY: WebRTC/WHEP (`:8889`), SECONDARY: RTSP (`:8554`), FALLBACK: HLS (`:8888`). | `VideoConnectionService.ts` resolves WebRTC/WHEP, RTSP, and HLS. | Native `expo-video` cannot decode WHEP HTTP signaling natively without `react-native-webrtc` / WHEP client. | Document exact player capabilities: native HLS/RTSP supported via `expo-video`; WHEP requires WebRTC signaling client. | Player audit + Stream URL unit test | **COMPLIANT** |
| **8** | **Video & Control Decoupling** | Separate status states (`controlStatus` vs `videoStatus`). Video drop never causes control disconnect. | `connectionSlice.ts` maintains independent lifecycles; zero video frames in Redux; video loss has zero failsafe impact. | None. | Maintain independent state machines and non-intrusive status pills. | Decoupled state unit test | **COMPLIANT** |
| **9** | **REST Admin Scope** | Port 8080 for `/api/status`, camera config, reboot CM5 only. | REST is decoupled from realtime flight control. | None. | Maintain strict separation: REST = Admin/Status, UDP = Flight Control. | Architectural review | **COMPLIANT** |
| **10** | **Wi-Fi ↔ 4G Handover** | Dynamic socket endpoint switching on network path change. | `ControlConnectionService.switchNetworkPath()` cleanly switches endpoints without app restart. | None. | Dynamically rebind target host/port upon network path change. | Network switch unit test | **COMPLIANT** |
| **11** | **No Hardcoded IPs/Secrets** | No `192.168.1.12`, no hardcoded secrets or auth tokens in service logic. | Fully configurable endpoints (`remoteHost`, `wifiHost`, `cloudHost`, `mediamtxHost`). | None. | Centralize default IPs in `src/settings/defaults/`. | Regex grep scan | **COMPLIANT** |
| **12** | **SafetyLayer & OTA Interlock** | Fail-closed command validation; OTA disabled when armed/flying. | `SafetyLayer.ts` & `updateSafetyService.ts` enforce strict fail-closed safety interlocks. | None. | Preserve existing safety checks and in-flight lockouts. | updateSafety test suite | **COMPLIANT** |
